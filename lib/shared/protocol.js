export const DEFAULT_PORT = 3000;

export const ENDPOINTS = {
  LIST: '/api/list',
  STAT: '/api/stat',
  DOWNLOAD: '/api/download',
  DOWNLOAD_DIR: '/api/download-dir',
};

export const PART_SUFFIX = '.part';
